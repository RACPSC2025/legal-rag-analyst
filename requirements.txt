# ── Core LLM / RAG ───────────────────────────────────────────────────────
langchain>=1.2.0
langchain-core>=1.3.0
langchain-community>=0.4.0
langchain-aws>=1.4.0
langchain-chroma>=1.1.0
langchain-text-splitters>=1.1.0
langgraph>=1.1.0

# ── AWS ───────────────────────────────────────────────────────────────────
boto3>=1.42.0
botocore>=1.42.0

# ── Vector Store ──────────────────────────────────────────────────────────
chromadb>=1.5.0

# ── PDF Processing ────────────────────────────────────────────────────────
pymupdf>=1.27.0          # PyMuPDF (fitz) — loader principal para ingesta
pymupdf4llm>=1.27.0      # Extractor Markdown avanzado
pdfplumber>=0.11.0       # pdfplumber — extractor Modo Mesa (limpieza por página)
docling>=2.9.0           # IBM Docling — tablas complejas → Markdown
unidecode>=1.4.0         # Limpieza de acentos y caracteres especiales
opencv-python>=4.13.0    # Preprocesamiento de imágenes (OCR)

# ── Retrieval Híbrido ─────────────────────────────────────────────────────
rank-bm25>=0.2.2         # BM25 sparse retrieval
flashrank>=0.2.10        # FlashRank reranking (ms-marco-MiniLM-L-12-v2)
numpy>=2.4.0

# ── Pydantic / Utils ──────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.14.0
python-dotenv>=1.2.0
typing-extensions>=4.15.0

# ── UI ────────────────────────────────────────────────────────────────────
streamlit>=1.38.0

# ── API REST (FastAPI) ────────────────────────────────────────────────────
fastapi>=0.136.0             # Framework web moderno y rápido
uvicorn[standard]>=0.46.0    # ASGI server con hot reload
sse-starlette>=3.4.0         # Streaming SSE para respuestas en tiempo real
python-multipart>=0.0.27     # Soporte para form data y file uploads
python-json-logger>=4.1.0    # Logging estructurado en JSON
httpx>=0.28.0                # Cliente HTTP para testing de endpoints

# ── Evaluación y Reporting ────────────────────────────────────────────────
rich>=15.0.0             # Console output bonito para evaluate_rag_health
pandas>=2.3.0            # DataFrames para reportes de evaluación

# ── Testing y Desarrollo ──────────────────────────────────────────────────
pytest>=9.0.0            # Framework de testing
pytest-asyncio>=1.3.0    # Testing asíncrono para FastAPI
hypothesis>=6.152.0      # Property-based testing para tests robustos

# ── Opcional: LlamaParse (pago, descomentar en producción) ────────────────
llama-parse>=0.6.0
# llama-index>=0.10.0

# ── Transformers (silenciar warnings) ─────────────────────────────────────
transformers>=5.7.0
