# ── Core LLM / RAG ───────────────────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-aws>=0.2.0
langchain-chroma>=0.1.4
langchain-text-splitters>=0.3.0
langgraph>=0.2.0

# ── AWS ───────────────────────────────────────────────────────────────────
boto3>=1.35.0
botocore>=1.35.0

# ── Vector Store ──────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── PDF Processing ────────────────────────────────────────────────────────
pymupdf>=1.24.0          # PyMuPDF (fitz) — loader principal para ingesta
pdfplumber>=0.11.0       # pdfplumber — extractor Modo Mesa (limpieza por página)
docling>=2.0.0           # IBM Docling — tablas complejas → Markdown

# ── Retrieval Híbrido ─────────────────────────────────────────────────────
rank-bm25>=0.2.2         # BM25 sparse retrieval
flashrank>=0.2.9         # FlashRank reranking (ms-marco-MiniLM-L-12-v2)
numpy>=1.26.0

# ── Pydantic / Utils ──────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
typing-extensions>=4.0.0

# ── UI ────────────────────────────────────────────────────────────────────
streamlit>=1.38.0

# ── API REST (FastAPI) ────────────────────────────────────────────────────
fastapi>=0.115.0             # Framework web moderno y rápido
uvicorn[standard]>=0.32.0    # ASGI server con hot reload
sse-starlette>=2.1.3         # Streaming SSE para respuestas en tiempo real
python-multipart>=0.0.12     # Soporte para form data y file uploads
python-json-logger>=2.0.7    # Logging estructurado en JSON
httpx>=0.27.2                # Cliente HTTP para testing de endpoints

# ── Evaluación y Reporting ────────────────────────────────────────────────
rich>=13.0.0             # Console output bonito para evaluate_rag_health
pandas>=2.0.0            # DataFrames para reportes de evaluación

# ── Testing y Desarrollo ──────────────────────────────────────────────────
pytest>=8.3.0            # Framework de testing
pytest-asyncio>=0.24.0   # Testing asíncrono para FastAPI
hypothesis>=6.0.0        # Property-based testing para tests robustos

# ── Opcional: LlamaParse (pago, descomentar en producción) ────────────────
# llama-parse>=0.5.0
# llama-index>=0.10.0

# ── Transformers (silenciar warnings) ─────────────────────────────────────
transformers>=4.40.0
