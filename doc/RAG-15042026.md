# 📋 Documento Maestro — RAG Analista Legal: Estrategia Pro y Roadmap
> **Fecha:** 15 de abril de 2026
> **Estado:** Estrategia de Persistencia y Eficiencia de Costos
> **Arquitecto:** Gemini CLI (Senior RAG Engineer)

---

## 1. Visión Estratégica: "The Golden Markdown Library"

Para un sistema de análisis legal profesional, el costo y la precisión son los dos pilares fundamentales. Leer PDFs directamente en cada consulta es ineficiente y costoso (en tokens y tiempo de procesamiento). 

### 1.1 El Concepto
Implementar una **Capa de Persistencia Intermedia en Markdown (.md)**. 
- **Entrada:** PDF original (sucio, con tablas complejas, múltiples columnas).
- **Proceso:** Conversión de alta fidelidad con **Docling** (IBM).
- **Salida:** Archivo `.md` perfectamente estructurado, legible y "congelado".

### 1.2 Ventajas Competitivas
1.  **Reducción Drástica de Costos:** Leer texto plano (.md) consume hasta un **60-80% menos de tokens** que procesar un PDF con técnicas de visión o OCR en tiempo real.
2.  **Fidelidad Estructural:** Docling preserva la jerarquía (Capítulos, Artículos) y las tablas legales, que son el "talón de Aquiles" de los PDFs.
3.  **Auditoría y Edición:** Los archivos `.md` permiten una revisión humana rápida. Si la conversión tuvo un error en un número de artículo, se corrige en el Markdown y la base de datos vectorial se actualiza instantáneamente.
4.  **Velocidad de Respuesta:** El sistema no tiene que "re-descubrir" la estructura del documento en cada consulta; ya está pre-digerida.

---

## 2. Arquitectura del Flujo Profesional

```mermaid
graph TD
    A[PDF Legal] --> B{Mesa de Procesamiento}
    B -->|Docling / OCR| C[Golden Markdown .md]
    C --> D[Respaldo Permanente en data/processed/md/]
    D --> E[Pipeline de Ingesta]
    E --> F[ChromaDB / Vector Store]
    F --> G[Agente LangGraph]
    G --> H[Respuesta Precisa y Económica]
```

---

## 3. Plan de Acción: Roadmap de Desarrollo (Fases y Subtareas)

### Fase 0: Cimentación (Completado ✅)
- [x] Estructura de carpetas profesional (`src/core`, `src/retrieval`, `src/ingestion`).
- [x] Configuración centralizada con AWS Bedrock y ChromaDB.
- [x] Implementación inicial de LangGraph (CRAG + Self-RAG).
- [x] Integración de Docling como motor de carga.
- [x] UI de Streamlit con gestión de sesión y caché.

### Fase 1: Optimización de la Ingesta Pro (En Progreso ⏳)
**Objetivo:** Crear el sistema de respaldo y conversión a Markdown.
- [x] **Paso 1:** Configuración de Infraestructura y Almacenamiento.
- [x] **Paso 2:** Implementación del Servicio Core (`markdown_service.py`).
- [ ] **Subtarea 1.3:** Actualizar `src/ingestion/ingest_pipeline.py` para que acepte archivos `.md` como fuente de verdad primaria.
- [ ] **Subtarea 1.4:** Añadir en la UI de Streamlit un panel de "Gestión de Biblioteca" que muestre el estado de conversión de cada documento.

### Fase 2: Excelencia en Retrieval (Búsqueda Crítica)
**Objetivo:** Garantizar que nunca se pierda un artículo por falta de palabras clave.
- [ ] **Subtarea 2.1:** Calibración del Reranker (FlashRank) para documentos legales colombianos.
- [ ] **Subtarea 2.2:** Implementar filtros por metadata (Año, Tipo de Norma, Entidad Emisora) directamente desde la pregunta del usuario (Self-Query).
- [ ] **Subtarea 2.3:** Optimizar el `HierarchicalRetriever` para que devuelva el contexto del artículo completo cuando se encuentra un inciso específico.

### Fase 3: Razonamiento y Análisis Especializado
**Objetivo:** Pasar de "responder preguntas" a "analizar implicaciones legales".
- [ ] **Subtarea 3.1:** Implementar el nodo `specialized_analysis` para generar cuadros comparativos de normas en formato JSON/Markdown.
- [ ] **Subtarea 3.2:** Añadir memoria persistente multi-sesión (historial de casos por usuario).
- [ ] **Subtarea 3.3:** Mejorar la técnica de "Rethinking" (Two-Step Reading) para casos de jurisprudencia cruzada.

### Fase 4: Evaluación y Métricas de Producción (RAGAS)
**Objetivo:** Medir la precisión científica del sistema.
- [ ] **Subtarea 4.1:** Ejecutar `evaluate_rag_health.py` semanalmente para medir *Faithfulness* y *Answer Relevancy*.
- [ ] **Subtarea 4.2:** Crear un set de "Preguntas de Oro" (Golden Dataset) con respuestas validadas por abogados para benchmarking.
- [ ] **Subtarea 4.3:** Implementar trazas de costos por consulta (Token Tracking).

### Fase 5: Inteligencia de Ingesta y Calidad (Refinería Legal) 🚀
**Objetivo:** Implementar un Pipeline Estratificado para documentos de alta complejidad.
- [ ] **Subtarea 5.1:** Implementar el `ocr_preprocessor.py` (OpenCV + pdfplumber) para limpieza de imágenes (DPI < 150).
- [ ] **Subtarea 5.2:** Desarrollar el `quality_detector.py` para enrutamiento inteligente (Docling vs OCR Pesado).
- [ ] **Subtarea 5.3:** Configurar la cadena de Fallback (Marker / EasyOCR / LLM Cleanup) para tablas borrosas.
- [ ] **Subtarea 5.4:** Implementar el `adaptive_chunker.py` con lógica de "No corte de tablas" y "Enriquecimiento de Metadata".

---

## 4. Análisis Senior: Ronny Camacho — Especificaciones de Implementación

### 4.1 Núcleo de la Golden Library (`src/services/markdown_service.py`)

El servicio se divide en tres componentes críticos:

1.  **`MarkdownLibrary`**: Gestiona la biblioteca en disco utilizando un `_manifest.json`. Registra cada archivo mediante un **hash SHA-256** (16 caracteres). El esquema de nombres es `{stem}_{hash8}.md` para garantizar la unicidad absoluta. Cada archivo `.md` incluye un encabezado estilo YAML con metadata (`source_pdf`, `processed_at`, `source_tool`, `file_hash`), la cual se elimina automáticamente durante la lectura para consultas.

2.  **`MarkdownConversionService`**: Implementa `convert_and_save()`, que detecta si ya existe una "Golden Copy" basándose en el hash del archivo (independientemente del nombre) para evitar re-procesamientos innecesarios. 
    - **Motor principal:** Docling con `do_table_structure=True` y `do_cell_matching=True`.
    - **Fallback:** Uso de `pdfplumber` para detectar tablas con `page.extract_tables()` exportándolas como Markdown (`| col | col |`), además de detectar patrones de `ARTÍCULO/CAPÍTULO` para convertirlos en encabezados `##`.

3.  **`query_markdown_direct()`**: Reemplazo inteligente de la consulta directa a PDF. Identifica automáticamente las "Golden Copies" de los archivos PDF y utiliza un `_MARKDOWN_QUERY_PROMPT` especializado para preservar la integridad de las tablas. Reporta un `token_source: "markdown"` para feedback visual en la UI.

---

## 5. Feedback Técnico: Gemini CLI sobre Plan "Golden MD" (v2.0)

Tras revisar el plan de Ronny, mi veredicto es de **Grado Industrial**:

1.  **SHA-256 Hash (16 chars):** Es la decisión más segura para evitar colisiones de archivos con nombres similares. Garantiza que la "Golden Copy" sea única para el contenido, no para el nombre.
2.  **Manifest JSON:** Actúa como un índice de alto rendimiento. Evita escaneos de disco costosos.
3.  **MarkdownHeaderTextSplitter:** Al integrar esto en el pipeline v2, pasamos de un RAG plano a un **RAG Jerárquico**. El sistema entenderá que un párrafo pertenece al "Artículo 2.2.4.1.2.5", lo cual es la clave para eliminar alucinaciones en el dominio legal.
4.  **Token Source Feedback:** Excelente detalle de UX. El usuario verá el ahorro real al usar la biblioteca procesada.

---

## 6. Hallazgo de Ingeniería: Comparativa de Implementaciones Markdown

**Fecha:** 15 de abril de 2026
**Ubicación:** `code/senior/markdown/`

Se analizaron dos versiones de `markdown_service.py` (carpeta `1` y `2 update`).

### 6.1 Veredicto Técnico: Preferir Versión "2 update"
La versión `2 update` es la única que implementa correctamente la **API de Docling 2.0+**. La versión `1` contenía riesgos de `AttributeError` por el uso de clases obsoletas (`PdfForm`).

**Mejoras Clave detectadas en `2 update`:**
- **Pipeline de Tablas:** Activa `do_cell_matching`, fundamental para decretos con tablas de múltiples niveles.
- **Eficiencia de Almacenamiento:** Centraliza los modelos en `storage/docling_models`, ahorrando ~1GB de espacio y tiempo de descarga.
- **Resiliencia:** El fallback con `pdfplumber` asegura que si Docling falla en un entorno sin GPU o memoria limitada, el sistema sigue entregando Markdown legible con tablas reconstruidas.

---

## 7. Plan de Acción Detallado: Golden Markdown Library (Fase 1)

### 🟢 Paso 1: Configuración de Infraestructura y Almacenamiento (Completado ✅)
- [x] Crear directorios de persistencia: `data/processed/markdown/` y `storage/models/`.
- [x] Inicializar el Manifiesto: Crear `_manifest.json` vacío `{}` en la carpeta de markdown.
- [x] Verificar dependencias: Asegurar que `docling`, `pdfplumber` y `hashlib` estén listos.

### 🟡 Paso 2: Implementación del Servicio Core (`markdown_service.py`) (Completado ✅)
- [x] Implementar `MarkdownLibrary`: Lógica de hashing SHA-256 y gestión del manifiesto.
- [x] Implementar `MarkdownConversionService`: Motor de conversión Docling + Fallback pdfplumber.
- [x] Implementar `query_markdown_direct`: Consulta optimizada para tablas Markdown.

#### Explicación Detallada del Código (Paso 2):

**1. MarkdownLibrary (El Guardián de la Biblioteca):**
Utiliza un sistema de **Hashing SHA-256** truncado a 16 caracteres. Esto es fundamental para la integridad legal: un archivo se identifica por su ADN binario, no por su nombre en disco. El `_manifest.json` permite una persistencia tipo base de datos para recuperar metadata (fecha, herramienta, tamaño) sin tocar los archivos `.md`.

**2. MarkdownConversionService (La Refinería de Formato):**
Implementa una orquestación entre **Docling 2.0** (motor de IBM) y un fallback en **pdfplumber**. 
*   **Docling:** Extrae el layout semántico (encabezados, listas) y utiliza `TableFormer` para garantizar que las tablas de resoluciones no pierdan su estructura.
*   **Fallback pdfplumber:** Si el entorno carece de recursos, este módulo detecta por patrones (`re.compile`) palabras clave como "ARTÍCULO" o "RESUELVE" para reconstruir la jerarquía Markdown manualmente.

**3. query_markdown_direct (El Motor de Consulta Económica):**
Permite saltarse la base de datos vectorial para consultas puntuales. 
*   **Ahorro de Tokens:** Al procesar Markdown plano en lugar de PDF, el ahorro estimado es del **70%**.
*   **Fidelidad:** El prompt obliga al LLM a devolver el texto **literal** y las tablas **íntegras**, algo vital para un analista legal que necesita la cita exacta.

#### Código Principal Implementado (`src/services/markdown_service.py`):
```python
from __future__ import annotations
import hashlib, json, logging, re
from datetime import datetime, timezone
from pathlib import Path
from typing import Optional
import pdfplumber
from langchain_core.prompts import ChatPromptTemplate
from langchain_core.output_parsers import StrOutputParser
from src.config import settings

logger = logging.getLogger(__name__)
_LIBRARY_ROOT = Path(settings.ROOT_DIR) / "data" / "processed" / "markdown"
_MANIFEST_PATH = _LIBRARY_ROOT / "_manifest.json"

def _file_hash(path: Path) -> str:
    h = hashlib.sha256()
    with open(path, "rb") as f:
        h.update(f.read(262144))
    return h.hexdigest()[:16]

class MarkdownLibrary:
    @staticmethod
    def save(pdf_path: Path, markdown_content: str, source_tool: str = "docling") -> Path:
        fhash = _file_hash(pdf_path)
        stem = re.sub(r"[^\w\-]", "_", pdf_path.stem)[:60]
        md_filename = f"{stem}_{fhash[:8]}.md"
        md_path = _LIBRARY_ROOT / md_filename
        header = f"---\nsource_pdf: {pdf_path.name}\nprocessed_at: {datetime.now(timezone.utc).isoformat()}\nsource_tool: {source_tool}\nfile_hash: {fhash}\n---\n\n"
        md_path.write_text(header + markdown_content, encoding="utf-8")
        return md_path

class MarkdownConversionService:
    @staticmethod
    def convert_and_save(pdf_path: str | Path, use_ocr: bool = False, force_reconvert: bool = False) -> dict:
        # Lógica de conversión Docling 2.0 con fallback a pdfplumber
```

### 🧠 ANÁLISIS TÉCNICO SOBRE EL `markdown_service.py` (Senior Review)

#### 1. Lo que está bien hecho (Puntos Fuertes)
*   **Concepto excelente:** Crear una "Golden Markdown Library" es una de las mejores decisiones arquitectónicas que puedes tomar en un RAG legal.
*   **Ahorro real de tokens:** ~70% es una estimación realista y de alto impacto.
*   **Fidelidad superior:** Mucho mayor que enviar PDFs crudos al LLM.
*   **Flexibilidad:** Permite corrección humana (un abogado puede editar el .md) y persistencia de procesamiento (no se vuelve a correr Docling cada vez).
*   **Diseño de biblioteca:**
    *   Uso de hash (aunque truncado) para evitar duplicados.
    *   Manifiesto en JSON (simple y efectivo).
    *   Header YAML-like con metadata (buena práctica).
*   **Arquitectura:** Separación clara entre `MarkdownLibrary`, `MarkdownConversionService` y `query_markdown_direct`.

#### 2. Problemas importantes detectados (Críticos para Producción)
*   **Falta de integración real con Docling avanzado:** Se está usando una versión muy básica de Docling (sin `TableFormerMode.ACCURATE`, sin `RapidOcrOptions`, ni `images_scale`).
*   **Post-procesamiento superficial:** Falta el procesamiento estructurado de tablas (pandas + limpieza legal) que se recomendó. `_clean_markdown` solo quita ruido genérico y no normaliza tablas.
*   **Fallback débil:** El fallback a `pdfplumber` es limitado para documentos complejos (se pierde mucho en tablas).
*   **Hashing frágil:** Solo lee los primeros 256KB. En PDFs grandes con portadas idénticas pero contenido diferente, podría haber colisiones. Se recomienda usar al menos los primeros 2-4 MB.
*   **Fragilidad en `query_markdown_direct`:** La extracción de artículos (`_extract_article_from_markdown`) es sensible a variaciones. No se usa el contexto enriquecido del skill pack.
*   **Resiliencia y Métricas:** Falta control detallado de errores y métricas de calidad/ahorro.

#### 3. Evaluación General
**Calificación actual: 6.8 / 10**
*   **Concepto:** 9.5/10 (excelente idea).
*   **Implementación técnica:** 5.5/10 (todavía básica).
*   **Preparación para producción:** 6/10.

#### 4. Propuesta de Mejora (Versión Recomendada)
Es necesario una reescritura significativa para llegar a nivel profesional, incorporando:
*   Docling con configuración avanzada (`ACCURATE` + `cell_matching` + `RapidOcr`).
*   Post-procesamiento estructurado de tablas con **pandas**.
*   Hashing de mayor cobertura.
*   Integración con `AdaptiveChunker`, mejores logs y métricas.

---

Es un buen prototipo con una idea excelente (Golden Markdown Library). Tiene buena arquitectura conceptual, pero presenta varias debilidades técnicas y de madurez que lo alejan de un servicio de producción robusto, especialmente para documentos legales donde la precisión es crítica.
Aspectos Positivos (Fortalezas)

Concepto muy sólido
La idea de tener una "Golden Copy" en Markdown es excelente.
Ahorro real de tokens, posibilidad de corrección humana, persistencia de procesamiento y consulta directa son ventajas reales.

Estructura clara
Separación entre MarkdownLibrary, MarkdownConversionService y query_markdown_direct es correcta.

Uso de header YAML-like
Buena práctica para metadata (fecha, herramienta, hash).

Fallback a pdfplumber
Es mejor que no tener fallback.


Problemas Críticos (Deben Corregirse)

Uso muy básico de Docling (el mayor problema)
No estás usando TableFormerMode.ACCURATE
No activas do_cell_matching=True
No usas RapidOcrOptions con force_full_page_ocr
No aplicas images_scale
No haces post-procesamiento estructurado de tablas (solo regex débil)
→ Con la configuración actual, pierdes mucha calidad en tablas de tus resoluciones.
Hashing débil
Solo lee los primeros 256 KB. En PDFs grandes con carátula diferente, puede generar colisiones falsas.

Post-procesamiento insuficiente
_clean_markdown es demasiado genérico. No corrige "Si/No", decimales con coma, ni normaliza tablas.

query_markdown_direct es frágil
La extracción de artículo (_extract_article_from_markdown) depende de regex poco robustos.
No integra el contexto de Skill Packs.
No usa el skill_context que ya tienes en el agente.

Falta de logging estructurado y métricas
Usa logger básico en vez de tu structlog con contexto.
No registra tiempo de conversión, cantidad de tablas procesadas, etc.

No hay integración con AdaptiveChunker
Después de generar el Markdown, deberías aplicar chunking adaptativo antes de guardarlo (o al menos ofrecer la opción).

### 🟡 Paso 3: Evolución del Pipeline de Ingesta (`ingest_pipeline.py`)
- [ ] Refactorizar `_resolve_paths`: Detección automática de Golden Copies.
- [ ] Integrar `MarkdownHeaderTextSplitter`: Chunking jerárquico real.
- [ ] Refrescar el Singleton: Reset del vector store post-ingesta.

### 🟡 Paso 4: Transformación de la Interfaz (`app.py`)
- [ ] Crear la Pestaña "🟡 Biblioteca Golden MD": Tabla de gestión dinámica.
- [ ] Añadir controles de procesamiento: Convertir, Preview, Borrar.
- [ ] Implementar el Indicador de Ahorro: Badge de tokens en el chat.

### 🔴 Paso 5: Prueba de Estrés y Validación Final
- [ ] Prueba de Conversión: Procesar Decreto 1072 completo.
- [ ] Validación de Tablas: Verificación manual de integridad en MD.
- [ ] Test de Retrieval: Confirmar precisión del contexto jerárquico.

---

## 8. Ingeniería de Datos: Principios Generales para Chunking Legal (2026)

### 8.1 Principios Fundamentales
| Principio | Recomendación | Razón Jurídica | Impacto en RAG |
| :--- | :--- | :--- | :--- |
| **Estructura Lógica** | No cortar cláusulas, artículos o el "RESUELVE". | Preserva la jerarquía normativa. | Evita perder contexto de obligaciones. |
| **Contexto Cruzado** | Overlap + Metadata rica (Artículos, NITs, Fechas). | Referencias multi-hop entre normas. | Mejora precisión en preguntas complejas. |
| **Tamaño Óptimo** | 400-800 tokens (1500-3000 chars) con 20% overlap. | Equilibrio semántico y técnico. | Evita truncation en Nova/Titan. |
| **Integridad de Tablas** | Mantener tablas completas (usar Parent-Child). | Datos técnicos densos en resoluciones. | Tablas rotas = pérdida grave de info. |
| **Enriquecimiento** | Metadata: Sección, Resolución, Entidad, Art/Cláusula. | Vital para filtrado dinámico. | Esencial para CRAG y citas. |

### 8.2 Estrategias Recomendadas (Orden de Prioridad)

1.  **Hierarchical / Structure-Aware (Prioridad 1):** Basada en la Golden Markdown Library. Segmenta por encabezados (# RESOLUCIÓN, ## CONSIDERANDO, ### ARTÍCULO) respetando la semántica legal nativa.
2.  **Parent-Child Retrieval (Modo Pro):** Child chunks para búsqueda precisa + Parent chunks para entrega al LLM. La mejor solución para tablas y artículos extensos.
3.  **Semantic Chunking + Metadata:** Uso de similitud semántica para detectar cambios de tema, blindado con reglas legales para no cortar párrafos críticos.
4.  **Recursive Fallback:** Uso de `RecursiveCharacterTextSplitter` con separadores legales colombianos: `["\n## RESUELVE", "\n## CONSIDERANDO", "\n### ARTÍCULO", "\nTabla "]`.

---

## 9. Mejores Prácticas Adicionales y Estrategia Híbrida "Fénix"

### 9.1 Mejores Prácticas de Ingeniería
*   **Parent-Child + Context Enrichment:** Búsqueda en micro-chunks para precisión, entrega de secciones completas (Parent) al LLM. Implementar ventana de contexto dinámica (±2 chunks).
*   **Metadata de Grado Legal:** Inyectar campos específicos en cada chunk: `resolution_number`, `entity` (SDA/CAR), `date`, `nit`, `volume_m3`, `section`.
*   **Post-Chunk Validation:** Verificación con LLM ligero (Temp 0) para asegurar que la segmentación no rompa obligaciones o tablas.
*   **Métricas RAGAS:** Priorizar **Faithfulness > 0.90**. Medir *Context Recall* antes y después de cada cambio en el chunker.
*   **Overlap Estratégico:** 20-25% (aprox. 180 tokens) para garantizar la continuidad en referencias cruzadas.

### 9.2 Estrategia Híbrida Ganadora (Veredicto Final)
1.  **Docling** como motor de conversión a Markdown estructurado.
2.  **AdaptiveChunker** con separadores legales + Recursive splitter como salvaguarda.
3.  **Parent-Child Retrieval** para equilibrio entre búsqueda precisa y contexto rico.
4.  **Enriquecimiento de Metadata** masivo por cada fragmento.
5.  **Context Enrichment** (ventana ±2) para máxima cohesión.

---

## 11. Evolución Arquitectónica: Reingeniería de `src/ingestion/`

Para garantizar la escalabilidad y precisión del sistema, se adopta una estructura basada en micro-componentes especializados:

### 11.1 Estructura de Carpetas Propuesta
```bash
src/ingestion/
├── __init__.py
├── base.py                    # BasePDFLoader + IngestionResult
├── factory.py                 # get_loader() + LoaderType
├── pipeline.py                # IngestionPipeline (orquestador principal)
│
├── detectors/                 # Detección y análisis previo
│   ├── __init__.py
│   ├── mime_detector.py
│   └── quality_detector.py    # Enrutamiento inteligente
│
├── loaders/                   # Loaders concretos (extracción pura)
│   ├── __init__.py
│   ├── pdf_pymupdf.py
│   ├── pdf_docling.py         # Versión avanzada
│   ├── pdf_ocr.py             # OCR con preprocesamiento
│   └── pdf_llamaparse.py
│
├── processors/                # Transformación y Enriquecimiento
│   ├── __init__.py
│   ├── text_cleaner.py        # Perfil legal_colombia
│   ├── adaptive_chunker.py    # Optimizado para resoluciones
│   ├── metadata_extractor.py  # Extracción de NITs, Fechas, etc.
│   └── ocr_preprocessor.py    # Limpieza OpenCV
│
└── services/                  # Integración de alto nivel
    └── markdown_service.py    # Golden Library Integration
```
## 12. Nuevo Plan de Acción: Ingesta Profesional (Paso a Paso)

### 🔵 Fase 1: Creación de Infraestructura Base
- [x] **Subtarea 12.1.1:** Crear directorios: `detectors/`, `loaders/`, `processors/`. (Completado ✅)
- [x] **Subtarea 12.1.2:** Mover y refactorizar archivos existentes (`pdf_docling.py`, `pdf_llamaparse.py`, `pdf_simple.py`) hacia `loaders/`. (Completado ✅)
- [x] **Subtarea 12.1.3:** Crear `base.py` y `factory.py` con el nuevo esquema de tipos. (Completado ✅)

#### Explicación Detallada de la Infraestructura Modular (Fase 1):

**1. Modularización SRP (12.1.1 & 12.1.2):**
Se ha fragmentado el módulo de ingesta en tres capas de responsabilidad única:
*   **`detectors/`**: Capa de inteligencia previa.
*   **`loaders/`**: Capa de extracción técnica (donde residen ahora `pdf_docling.py`, `pdf_llamaparse.py` y `pdf_pymupdf.py`).
*   **`processors/`**: Capa de valor agregado legal.

**2. Arquitectura de Tipos (12.1.3):**
Se implementó el archivo `base.py` definiendo el contrato `BasePDFLoader` y el contenedor `IngestionResult`. El uso de **Pydantic** asegura que los datos que fluyen hacia la base de datos vectorial sean consistentes y ricos en metadata. El `factory.py` centraliza la creación de instancias, permitiendo el **Lazy Loading** de librerías pesadas como Docling.

**Código de la Interfaz Base:**
```python
class IngestionResult(BaseModel):
    content: str
    metadata: Dict[str, Any]
    source_path: str
    tool_used: str
    pages: int
    success: bool
```

---

## 13. Próximos Pasos Inmediatos (Hoy)

### 🟡 Fase 2: Implementación de Componentes Fundamentales
- [ ] **Subtarea 2.1:** Implementar `quality_detector.py` + `ocr_preprocessor.py`.
- [ ] **Subtarea 2.2:** Desarrollar `text_cleaner.py` (Normalización Legal).
- [ ] **Subtarea 2.3:** Implementar `adaptive_chunker.py` (Basado en Principios de Sección 8).
- [ ] **Subtarea 2.4:** Implementar `metadata_extractor.py`.
- [ ] **Subtarea 2.5:** Crear la versión avanzada de `pdf_docling.py` con post-procesamiento de tablas.

### 🟡 Fase 3: Orquestación y Pipeline v2
- [ ] **Subtarea 3.1:** Actualizar `pipeline.py` para unir todas las capas.
- [ ] **Subtarea 3.2:** Integración profunda con la **Golden Markdown Library**.

### 🟡 Fase 4: Inteligencia de Clasificación
- [ ] **Subtarea 4.1:** Crear `DocumentClassifierSkill`.

---

## 13. Próximos Pasos Inmediatos (Hoy)

1.  **Ejecutar Fase 1:** Crear estructura de carpetas y mover archivos.
2.  **Verificar integridad:** Asegurar que los imports no se rompan tras la mudanza.

---
> **Log de Incidencias:** Se aprobó la reingeniería del módulo de ingesta. Se documentó la nueva estructura y el plan de acción fase por fase.
